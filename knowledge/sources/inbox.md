# Discovery inbox

New candidate sources surfaced by the auto-research **discovery** beat (see
[CLAUDE.md §4](../CLAUDE.md#4-operations)). Each row is raw capture awaiting compilation into
a wiki page + a registry id by the next agent ingest pass. The engine appends rows here and
dedupes against URLs already in this file and in `registry.md`.

| found | source | candidate finding | date | url |
|---|---|---|---|---|
